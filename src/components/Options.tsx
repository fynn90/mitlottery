import React from 'react';
import { SelectPrize, SelectExtractionNumber } from '../containers'

const Options = () =>(
  <section id="optionArea">
    <SelectPrize />
    <SelectExtractionNumber />
  </section>
);

export default Options;